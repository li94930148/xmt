import type{BrowserInfo,BrowserSelection,BrowserSession}from'./types.js';import{ManagedBrowserSession,ExternalCdpSession}from'./browserSession.js';import{managedProfile}from'./profile.js';
export class BrowserRegistry{constructor(private dataRoot:string,private found:BrowserInfo[]){}list(){return[...this.found];}get(id:string){return this.found.find(item=>item.id===id);}create(selection:BrowserSelection,accountId:string):BrowserSession{const info=this.get(selection.id);if(!info)throw new Error(`浏览器 ${selection.id} 不可用，请重新执行浏览器发现`);if(selection.sessionMode==='connect-cdp'){if(!selection.cdpEndpoint)throw new Error('外部 CDP 模式缺少连接地址');return new ExternalCdpSession(info,selection.cdpEndpoint);}return new ManagedBrowserSession(info,selection,managedProfile(this.dataRoot,selection,accountId),this.dataRoot);}}
