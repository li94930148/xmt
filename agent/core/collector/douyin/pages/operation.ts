import type { Page } from 'playwright'; import type { NetworkCapture } from '../../../types.js'; import { clickLabels, openPage } from './helpers.js';
export async function collectOperation(page: Page, captures: NetworkCapture[]) { const start=captures.length; await openPage(page,'https://creator.douyin.com/creator-micro/data-center/operation'); const ranges:Record<string,unknown>={}; for(const label of ['今日','昨日','近7天','近30天']){const before=captures.length;await clickLabels(page,[label]);ranges[label]=captures.slice(before).map(c=>c.response);} return {ranges,raw:captures.slice(start).map(c=>c.response)}; }
