import crypto from 'node:crypto';import { encrypt,sign } from '../crypto/envelope.js';import type { AgentConfig,CreatorSnapshot } from '../types.js';import type {PageCapability} from '../explorer/pageExplorer.js';import {toUnifiedCreatorPayload} from './unifiedPayload.js';
async function responseJson(response:Response){const value=await response.json().catch(()=>({})) as Record<string,unknown>;if(!response.ok)throw new Error(String(value.message||`请求失败 HTTP ${response.status}`));return value;}
export async function loginXmt(serverUrl:string,username:string,password:string){const response=await fetch(`${serverUrl.replace(/\/$/,'')}/api/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username,password})});return responseJson(response) as Promise<{token:string;user?:unknown}>;}
export async function register(serverUrl:string,webToken:string,accountId:string,deviceId:string){const response=await fetch(`${serverUrl.replace(/\/$/,'')}/api/creator-agent/register`,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${webToken}`},body:JSON.stringify({platform:'douyin',account_id:accountId,device_id:deviceId})});return responseJson(response) as Promise<{agent_id:number;agent_token:string}>;}
export async function bind(serverUrl:string,bindingCode:string,device:Record<string,unknown>){const response=await fetch(`${serverUrl.replace(/\/$/,'')}/api/creator-agent/bind`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({binding_code:bindingCode,device})});return responseJson(response) as Promise<{agent_id:number;agent_token:string;account_id:string}>;}
export async function heartbeat(config:AgentConfig,agentToken:string,state:{deviceName:string;os:string;browserLoginStatus:string}){const browser=config.browserConfig;const response=await fetch(`${config.serverUrl.replace(/\/$/,'')}/api/creator-agent/heartbeat`,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${agentToken}`},body:JSON.stringify({agent_id:config.agentId,account_id:config.accountId,device_name:state.deviceName,os:state.os,agent_version:'2.11.0-agent',protocol_version:1,browser_login_status:state.browserLoginStatus,browser_type:browser.type,browser_version:browser.browserVersion||'',browser_engine:browser.engine,browser_runtime:browser.runtime,session_mode:browser.sessionMode,compatibility_status:browser.compatibilityStatus||'not_tested'})});return responseJson(response);}
export async function upload(config:AgentConfig,agentToken:string,snapshot:CreatorSnapshot,options:{knownContentIds?:Set<string>;capabilities?:PageCapability[];taskId?:string}={}){const body:Record<string,unknown>={protocol_version:1,agent_version:snapshot.agent_version,agent_id:config.agentId,device_id:config.deviceId,platform:config.platform,account_id:config.accountId,timestamp:new Date().toISOString(),nonce:crypto.randomUUID(),collected_at:snapshot.collected_at||new Date().toISOString(),data:encrypt(toUnifiedCreatorPayload(snapshot,options),agentToken)};body.signature=sign(body,agentToken);const serialized=JSON.stringify(body);const clientPayloadBytes=Buffer.byteLength(serialized);if(clientPayloadBytes>12*1024*1024)throw new Error('同步数据包超过 12MB 限制，请缩小采集范围后重试');const response=await fetch(`${config.serverUrl.replace(/\/$/,'')}/api/creator-agent/data-sync`,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${agentToken}`},body:serialized});const result=await responseJson(response) as {success:boolean;status:'success'|'partial_success'|'failed';success_count:number;failed_count:number;modules:Record<string,'success'|'failed'>;errors:Record<string,string>;snapshot_time?:string;account_id?:number;contents?:number;metrics?:number;trends?:number;raw_records?:number};return{...result,client_payload_bytes:clientPayloadBytes};}
