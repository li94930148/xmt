import{useState}from'react';import type{SetupInput}from'../types';
export default function Login({onSetup}:{onSetup:(input:SetupInput)=>Promise<void>}){const[input,setInput]=useState<SetupInput>({serverUrl:'https://lanyaomedia.com',username:'',password:'',accountId:''});const[busy,setBusy]=useState(false);const[error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await onSetup(input);}catch(cause){setError(cause instanceof Error?cause.message:'连接失败');}finally{setBusy(false);}};return <div className="onboarding"><div className="brand"><div className="logo">X</div><div><h1>XMT Creator Agent</h1><p>安全连接您的 XMT 工作空间</p></div></div><form className="card form" onSubmit={submit}><label>XMT 服务器地址<input value={input.serverUrl} onChange={e=>setInput({...input,serverUrl:e.target.value})} placeholder="https://lanyaomedia.com"/></label><label>XMT 用户名<input value={input.username} onChange={e=>setInput({...input,username:e.target.value})} autoComplete="username"/></label><label>XMT 密码<input type="password" value={input.password} onChange={e=>setInput({...input,password:e.target.value})} autoComplete="current-password"/><small>密码仅用于本次登录，不会保存到本地。</small></label><label>抖音账号 ID<input value={input.accountId} onChange={e=>setInput({...input,accountId:e.target.value})} placeholder="用于绑定本设备"/></label>{error&&<div className="error">{error}</div>}<button disabled={busy}>{busy?'正在安全连接…':'登录 XMT 并绑定设备'}</button></form></div>}
