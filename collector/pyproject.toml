[project]
name = "xmt-creator-collector"
version = "2.12.0"
description = "XMT Creator Agent 的 Scrapling First 本地采集 Worker"
requires-python = ">=3.10"
dependencies = ["scrapling[fetchers]==0.4.14"]

[project.optional-dependencies]
test = ["pytest==8.4.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
