app.example.com {
    encode zstd gzip
    # Metrics are scraped directly from the private Node listener. Never proxy
    # the internal endpoint through the public site.
    respond /internal/metrics/auth 404
    reverse_proxy 127.0.0.1:3001

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        X-Content-Type-Options "nosniff"
        Referrer-Policy "strict-origin-when-cross-origin"
        -Server
    }

    log {
        output file /var/log/caddy/xmt-access.log {
            roll_size 10MiB
            roll_keep 10
            roll_keep_for 720h
        }
    }
}
