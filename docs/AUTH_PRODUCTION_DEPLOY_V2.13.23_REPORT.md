# Auth 生产部署 v2.13.23 报告

> 执行日期：2026-08-01  
> 目标版本：v2.13.23  
> 目标提交：`155793b`（架构: 完成Production Socket Bridge受控准入）

## 结论

**部署未执行，已在代码同步校验阶段安全停止。**

生产服务器从 `origin` 执行远端同步后，仍不存在目标提交 `155793b`。为避免部署未知或不完整代码，未执行 checkout、依赖安装、构建、PM2 部署、服务重启或任何灰度配置变更。

## 部署前检查

| 项目 | 结果 |
| --- | --- |
| 生产目录 | `/www/wwwroot/xmt`，确认存在 |
| Git 工作区 | 干净 |
| 当前分支 | `main` |
| 部署前提交 | `6390636b5983feee824709195a98946a461e85d2` |
| 目标提交远端可用性 | 不通过；`origin` 同步后仍无法解析 `155793b` |
| 最近数据库备份 | `data/backups/xmt-2026-08-01-08-43-20.db` |
| 备份完整性 | `PRAGMA quick_check` 返回 `ok` |

## 服务与数据库状态

本次未改变生产运行状态。部署前预检记录的状态仍适用：

- 运行版本：v2.13.12
- API 健康接口：通过
- SQLite 主库：`PRAGMA quick_check` 通过
- Login Rollout：关闭
- Socket Bridge：关闭
- Socket Bridge 审批：关闭

## 未执行项

- 未切换至 v2.13.23
- 未执行生产构建或 PM2 部署
- 未执行 `npm run auth:production-preflight`（当前 v2.13.12 不含该脚本）
- 未开启 Login Rollout、Socket Bridge 或审批门禁
- 未创建测试账号、修改 allowlist 或变更数据库数据

## 风险项

1. 目标提交尚未发布到生产可拉取的 Git 远端，当前是唯一部署阻断项。
2. 在上次预检中，PM2 累计重启 61 次且堆内存使用约 92%；完成部署后必须复核。
3. Caddy 日志曾有 Socket.IO 到 `127.0.0.1:3001` 的 502；完成部署后必须观察是否仍持续出现。

## 下一步

将本地 `main` 上的 `155793b` 发布到受控远端后，重新执行本阶段部署流程：确认远端提交、同步生产代码、构建、部署、健康检查及 `npm run auth:production-preflight`。在新的明确灰度指令前，所有 Auth 与 Socket 灰度门禁必须保持关闭。
