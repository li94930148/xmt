# XMT 系统版本

## 当前版本

v3.0.3

## 版本规则

- 工程版本以 `package.json` 为准。
- 每次升级递增一个明确版本，不跳过中间版本。
- 版本变更需同步 README、根目录 CHANGELOG、`docs/CHANGELOG.md` 与系统更新说明。

## 本版本摘要

本版本（v3.0.3）让每日与 12 小时自动任务固定使用已验证的官方四文件采集路径，避免全量滚动异常阻断定时导出。

上一版本（v3.0.2）兼容抖音对象形态的作品状态，避免普通作品快照因字段类型变化而部分失败。
