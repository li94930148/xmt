# XMT 系统版本

## 当前版本

v2.20.24

## 版本规则

- 工程版本以 `package.json` 为准。
- 每次升级递增一个明确版本，不跳过中间版本。
- 版本变更需同步 README、根目录 CHANGELOG、`docs/CHANGELOG.md` 与系统更新说明。

## 本版本摘要

本版本（v2.20.24）统一活动日志及页面日期的北京时间口径。活动日志升级时校正历史 UTC 值，后续显式写入北京时间，并只保留最近 7 天。

在本版本之前（v2.20.23）完成统一创作资料草稿。
