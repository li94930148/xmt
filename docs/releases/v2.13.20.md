# XMT v2.13.20 发布说明

## 阶段

Phase 2-C3-8-A：Auth + Socket + Yjs 真实浏览器闭环验证。

## 验证内容

- 使用真实 Chromium 验证 v1-web 登录、HttpOnly Refresh Cookie 和页面刷新恢复。
- 验证 Access Token 刷新后 Runtime 更新、Socket 重握手、Room 恢复和 Yjs state vector 一致。
- 验证 Awareness、Lock、Logout 和双标签状态同步。
- 修复 Playwright 缓存版本不一致和 opaque 页面 Origin 问题。

## 兼容边界

- 未修改正式 Login、legacy Socket、Yjs 协议、数据库或生产配置。
- 生产仍保持 legacy，Coordinator 和 v1 Socket 未开启。
