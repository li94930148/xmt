# Creator Agent v2.13.6-agent 发布说明

- 对应系统版本：`v2.20.11`
- 范围：本地封面 metadata-only 的来源发现与脱敏诊断。

修复 `creator.douyin.com` XHR 捕获正则的双重转义；捕获为空时返回 `COVER_METADATA_SOURCE_NOT_FOUND`，不再显示为成功的作品 0。结果由 Main、Bridge、Preload 和 Renderer 严格投影为阶段名、固定状态码和聚合计数，不含 URL、账号、作品或登录资料。

普通同步、上传、队列、batch、快照、Excel 下载及业务数据库写入保持独立。本发布尚未安装正式 App，也未执行第二次真实 metadata-only 检查。
