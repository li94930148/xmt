# Creator Agent v2.13.7-agent 发布说明

对应系统版本 `v2.20.12`。本地封面来源检查新增 HEAD、有限 Range GET 与固定 Creator Referer 对照，以及 `REMOTE_FORBIDDEN`、`REFERER_BOUND`、`HEAD_UNSUPPORTED_GET_VALID` 等脱敏来源分类。

该功能不发送认证信息、不上传数据、不持久化候选或图片；真实来源分类复验需另行授权。
