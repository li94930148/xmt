# v2.14.2 发布说明

## Phase 2-C3-8-C3.1：灰度配置来源统一与运行态门禁校验

- Auth 灰度相关模块统一使用 PM2 进程启动时的配置快照。
- 管理诊断增加有效配置来源、Auth v1/Web、Login Rollout、Socket Bridge、allowlist 数量、进程 ID 与加载时间。
- 灰度 readiness 改为读取实际运行中的本机诊断端点；无法读取运行态时安全返回 `NOT_READY`。
- 无数据库变化；生产灰度默认保持关闭。
