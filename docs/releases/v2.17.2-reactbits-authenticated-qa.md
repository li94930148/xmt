# XMT V2.17.2 React Bits 认证态兼容测试补充

## 变更说明

本版本仅补充本地认证态兼容测试，不修改业务功能、后端 API、数据库、权限、Workflow、Socket.IO 或 Yjs。

- 测试凭证仅从执行进程的 `XMT_E2E_USERNAME`、`XMT_E2E_PASSWORD` 与 `XMT_E2E_BASE_URL` 读取。
- 缺少认证环境变量时，测试安全跳过且不包含任何默认账号或密码。
- 测试目标只接受 `localhost` 或 `127.0.0.1`，覆盖 React Bits 双主题、预设、字号、视口和配置恢复场景。
