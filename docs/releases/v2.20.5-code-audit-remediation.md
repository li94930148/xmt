# v2.20.5 代码审计整改记录

审计输入：`outputs/code-review-2026-08-28.md`。本记录以当前代码和本地依赖解析结果为准，不将报告中的建议直接视为已证实缺陷。

## 已整改并验证

| 审计项 | 当前整改 | 验证 |
| --- | --- | --- |
| P0-1 错误响应泄露 | `/api` 在输出 5xx 前统一移除可枚举 `error` 字段，保留业务 `message` 和 `INTERNAL_ERROR`；日志只记录 requestId 与错误类型，不记录 SQL/参数。 | 脱敏合同确认 JSON 不含 SQL/敏感参数。 |
| P0-2 `xlsx` | 改用 SheetJS 官方 CDN `0.20.3`，保留真实 `.xlsx` 导出兼容；解析前限制 10MB、20000 行、200 列。 | `npm audit --omit=dev` 为 0 漏洞。 |
| P0-3 React Router | `react-router-dom` 升至 `7.18.2`。 | 生产依赖审计为 0 漏洞。 |
| P0-4 Socket 依赖/DoS | 精确锁定 `ws`、`socket.io-parser`、`engine.io`、`engine.io-client`、`socket.io-adapter` 的修复版本；单个 Socket payload 限制为 1MB，ping timeout 为 20 秒。 | Socket 生命周期合同通过；生产依赖审计为 0 漏洞。 |
| P1-5 IP 解析 | `ip-address` 锁定至 `10.5.0`。 | Internal loopback 合同通过；生产依赖审计为 0 漏洞。 |
| P1-6 Workflow 列表 | 拍摄/发布使用受限 `LIMIT/OFFSET`、总数和默认 50 条页大小；前端提供上一页/下一页。 | 分页边界合同通过。 |
| 其他低风险项 | 增加 Helmet 头、修正 16MB 文案、资源列表计数移除 SQL 正则截断、权限不足响应不再返回完整权限清单、权限缓存最多 1000 项。 | 类型检查与构建通过。 |

## 已核验、但不在本版改动

| 审计建议 | 结论与边界 |
| --- | --- |
| `bcrypt` 换为 `bcryptjs` | 不是已知漏洞；认证路径在生产运行时直接依赖原生 `bcrypt`。替换会改变性能和密码实现路径，需单独兼容性评审。 |
| Express 5 | 属跨主版本框架迁移，不是本次已证实漏洞的必要条件。 |
| 合并 `motion`/`framer-motion` | 两包当前都有真实导入，需先做视觉、包体和运行时回归，不能只删除依赖。 |
| 拆分巨型组件、Zustand/Query 归一 | 可维护性治理项，未发现本次审计范围内的可直接修复安全故障；应分模块独立排期。 |
| 全量 lint 入 CI、统一 `npm test` | 当前存在历史 lint 债务和多类环境测试；需先建立可通过的基线，避免新增一个恒失败门禁。 |
| 清理 `debug:*` 与 `data/` 备份 | 前者仍服务于 Creator 官方导出排障；后者为未跟踪本地数据，未经明确授权不删除或移动。 |
| 生产改为编译产物运行 | 部署架构调整，需在部署脚本、PM2 与回滚路径一起验证。 |

## 未执行的外部操作

- 未提交、推送、创建 PR、合并或部署。
- 未修改数据库、运行时密钥、Creator 登录资料或本地备份文件。
