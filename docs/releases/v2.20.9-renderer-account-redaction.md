# XMT v2.20.9 Creator Agent Renderer 账号标识脱敏

- System：`v2.20.9`
- Creator Agent：`v2.13.4-agent`

## 修复范围

主进程保留绑定账号、设备和令牌的内部解析。Renderer 仅接收绑定状态、平台标签、设备键控的 10 位短审计标识与范围状态；不会接收原始账号 ID、UID、数据库主键、设备 ID、完整昵称或完整配置对象。

`agent:get-state` 与推送的 `agent:state` 都在 preload 进行运行时白名单投影。未知字段、日志和错误上下文被丢弃，metadata-only 与普通同步仍由 Main 根据当前绑定执行。

## 数据与回滚

无数据库迁移、无生产数据变更。回滚时回滚本版本合并提交；无需处理数据库或 Profile。
