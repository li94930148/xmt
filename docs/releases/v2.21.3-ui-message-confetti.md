# v2.21.3 日常体验优化

## 范围

- 增强内容里程碑礼炮效果。
- 修复主按钮在不同浏览器中的黑边、白框和文字显示异常。
- 日报通知统一使用中文，并在读取时兼容既有英文消息。
- 移除页面标题和成片版本区的解释型说明文字。

## 数据与兼容

- 无数据库迁移，不修改或删除历史消息。
- 不改变审核、保存、版本、协同、权限或内容生产状态流转。

## 验证

- TypeScript、版本一致性、生产构建和测试入口通过。
- Chromium 主题矩阵与亮色对比度通过。
- WebKit 按钮矩阵和礼炮效果通过。

## 回滚

- 回滚应用代码即可；无数据库回滚步骤。
