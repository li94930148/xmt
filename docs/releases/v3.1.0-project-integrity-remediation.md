# v3.1.0 项目完整性整改

依据用户提供的 `project-integrity-audit.md` 审计报告逐条回查当前源码。审计文档中的阶段范围与建议不是本次实现指令；以现有调用链和数据契约为准。

## 本阶段已修

| 审计项 | 结果 |
| --- | --- |
| F-D1-01、F-D1-02、F-D10-02 | 强制改密服务端 403；权限检查统一由角色授权表决定，内置账号缺失映射时补齐；`/api/auth` 进入共享限流。 |
| F-D9-04 | 旧资源接口写入要求 `resource:create/update/delete`，并拒绝危险路径；已有归属校验保留。 |
| F-D5-06 | 协作房间编辑只允许管理员、主管、选题创建人和负责人；仅参与创作记录的操作人可查看但不能编辑，未分配的内容角色无法写入。 |
| F-D5-01 | 无权加入协作房间时返回错误事件，移除只检查 ID 与非空房间的占位函数。 |
| F-D2-02 | 选题删除及旧关联清理在同一事务中，失败回滚。 |
| F-D3-02 | 创作列表和详情使用相同的正文回退规则，避免列表取到旧字段。 |
| F-D9-01 | 番茄钟使用真实服务端开始、完成、放弃和统计；计数只在完成成功后刷新。 |
| F-CT-01、F-CT-02、F-CT-03 | 旧抖音技术入口转到现有运营中心；未挂载社媒复盘前端路由关闭；创建选题正确读取响应信封。 |
| F-D7-01、F-D7-02 | Collector 把脱敏且有上限的 XHR 记录送入 Agent 快照和统一上传载荷；本地看板、粉丝、原始响应缺失时状态为失败。 |
| F-D6-03、F-D10-01、F-D6-07、F-D4-04 | 导出、资料中心和旧资源接口未知异常返回固定安全文案；Agent 心跳校验协议版本；日报归档入口按权限码验证。 |
| F-D8-01 | Android 应用关闭系统备份，避免偏好设置中的凭据进入设备备份。 |
| 审计 P0 浏览器回归 | 系统更新提示改为页面角落卡片，不再用全屏遮罩阻挡业务操作。 |
| 验证基线 | 修复 Session 迁移测试对 `005` 必须为最后迁移的过期断言。 |
| F-D1-03、F-D1-04 | legacy JWT 加入 `auth_version` 吊销版本；退出、改密、禁用和角色变更立即使旧令牌失效，并清理权限缓存。 |
| F-D2-01、F-D2-03、F-D3-01、F-D3-04 | 选题和生产写入统一权限码；legacy/v1 共用校验 schema；移除废弃的 `workflow:production` 词条。 |
| F-D3-03、F-D3-05 | 生产历史改为分页与按需正文；补齐拍摄和发布 CRUD 回归。 |
| F-D4-01、F-D4-02、F-D4-03 | `/api/reports` 返回弃用标头；复盘权限进入自动回归；报告摘要用结构化校验错误。 |
| F-D5-02～F-D5-05 | 协作锁和事件落库并限制查询量；编辑失败原因分型；dashboard/timeline/replay 复用文档访问检查。 |
| F-D6-04～F-D6-08 | 同步优先平台作品 ID、写回同步时间；移除账号特例；协议心跳校验；加入前端入口与挂载契约。 |
| F-D7-03～F-D7-06 | Worker RPC 与上传协议分开命名和校验；设置、首次绑定、重新绑定统一校验 TLS；采集与上传契约纳入测试。 |
| F-D8-02～F-D8-04 | Android 安全凭据、备份禁用和深链清单加入契约测试；前后端共享深链常量。 |
| F-D9-02～F-D9-07 | 增加模板管理页；旧资源写接口返回 410 并引导资源中心；公开设置移除版本号；团队排行脱敏；登记模板和番茄钟 OpenAPI。 |
| F-D10-01～F-D10-05 | 收敛路由原始异常回传；归档根目录审计脚本；增加 API 覆盖矩阵和依赖覆盖说明；全仓 lint 恢复零错误并进入 CI。 |
| F-CT-04～F-CT-14 | 删除旧抖音抓取客户端和不匹配的共享类型；修正 latest snapshot 路径；补齐 7 个 v1 OpenAPI 操作；内部诊断路由在覆盖矩阵中标注。 |

## 边界和后续

- 未使用真实抖音账号执行线上采集验收；本次证据覆盖脱敏原始记录从 Collector 到 Agent 上传载荷的本地契约。
- 社媒复盘原后端因迁移故意未挂载；本次关闭前端死入口，未恢复旧服务或删除历史数据。
- lint 现为零错误并进入 CI；338 条历史质量提示仍以 warning 保留，后续可逐步消除，不再阻断构建。
- 本次不直接改现有生产数据库，也未部署。新建数据库启动时会补齐四个内置账号缺失的角色映射。

## 验证

- `npm run check`、`npm run build`、`npm run test:entrypoints`、`npm run test:auth`、`npm run test:rbac-invariant`、`npm run test:collaboration-access`、`npm run test:collaboration-socket-authorization`、`npm run test:topics`、`npm run test:rate-limit`。
- `npx tsx tests/integrity/resource-pomodoro.test.ts`；`npm run test:auth-session-migration`；`npm run test:api-contract`。
- Agent `npm run check`、`npm run build`、构建后的采集桥接测试；Collector 39 条 pytest。`npm run version:check` 通过。
- Playwright 本地模拟服务：运营周报/认证就绪页、官方逐日趋势页及番茄钟桌面和手机交互通过。两组现有浏览器用例需串行运行，避免 Vite HMR 端口冲突。
- `npm run lint` 零错误；新增 `npm run test:integrity` 覆盖历史分页、资源与番茄钟、模板、工作流、复盘、协作 dashboard 和前端挂载关系。构建仅有既存大 chunk 提示。
- 模板管理页完成 1440×1000 浏览器验收，权限守卫、列表展示和编辑回填通过。
