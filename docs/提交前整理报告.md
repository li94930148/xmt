# 提交前整理报告

本文档用于第 11 轮提交前整理。本轮不执行 Git commit。

## 当前改动模块分类

1. 编辑器主题与编码修复。
2. 布局滚动修复。
3. 日报后端。
4. 日报前端。
5. 日报增强。
6. 数据复盘后端。
7. 数据复盘前端。
8. 数据复盘增强。
9. 测试与 CI。
10. 上线、回滚、验收和提交前文档。

## 第 11 轮最终确认

- `npm run check`：通过。
- `npm run build`：通过。
- `npm run lint`：失败，仍为既有 238 个问题。
- 专项 eslint：通过。
- 日报 smoke：真实 token 链路通过。
- 复盘 smoke：真实 token 链路通过。
- E2E：登录态分支通过。
- 敏感信息扫描：未发现真实 token、真实密码或私钥写入仓库文件。

## 建议拆分 Commit

1. `fix: improve editor theme and layout scrolling`
2. `feat: add daily report backend and frontend`
3. `feat: add retrospective backend and frontend`
4. `feat: add retrospective action workflow and exports`
5. `test: add smoke tests and ci baseline`
6. `docs: add rollout and validation documents`

## 不建议提交文件清单

不建议提交：

- token。
- 密码。
- 临时日志。
- 本地数据库文件。
- Playwright 浏览器二进制。
- 构建产物。
- `.env`。
- 其他敏感配置。

第 11 轮安装的 Playwright Chromium 位于用户本机缓存目录，不在仓库内。

## 当前提交建议

- 可以进入 Git commit 准备阶段。
- 不建议直接 push GitHub，除非用户确认分支、提交拆分和 PR 目标。
- 不建议立即生产部署；生产前应先完成手工深度浏览器验收，或由用户明确接受该部分未补测风险。

## 第 12 轮中文化专项

- 本轮在前 6 个 commit 之后继续清理用户可见文案。
- 建议单独提交：`fix: polish Chinese UI copy and smoke data labels`。
- 提交范围应只包含用户可见中文文案、smoke 测试数据中文化、导出文案中文化、messages 提醒中文化和中文化清理报告。
- 不应混入新功能、数据库 schema、生产配置、真实账号、密码或 token。

## 第 12 轮最终提交前确认

- `npm run check`：通过。
- 非沙箱 `npm run build`：通过。
- `npm run lint`：失败，仍为既有 238 个问题，不作为本轮提交阻塞项。
- 专项 eslint：通过。
- 日报 smoke：真实链路通过。
- 复盘 smoke：真实链路通过。
- E2E：登录态分支通过。
- 浏览器深度补测：已覆盖日报、复盘列表、复盘详情、Markdown 导出、HTML 打印页、暗色模式和移动端窄屏，未发现应用级 console error。
- 用户可见英文复扫：剩余命中为内部实现、权限码、API 路径、数据库字段、CLI 日志或技术文档，不属于本轮用户界面文案。
- 敏感信息扫描：未发现真实 token、真实密码或私钥；未发现 `.env`、`data/xmt.db`、`dist`、Playwright 浏览器二进制或临时日志进入待提交范围。
- 建议执行单独 commit：`fix: polish Chinese UI copy and smoke data labels`。
