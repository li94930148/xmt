# 文档索引

## 文档目的

本文档是 XMT 项目的 docs 入口索引，用于配合 `Codex开发规范.md` 中的 Docs-first / Docs-last 闭环。

后续修改项目时，应先按任务类型定位需要阅读的文档，再检查当前代码真实状态，最后在代码变更后同步更新相关文档。

## 总览与路线

| 文档 | 适用场景 | 说明 |
|---|---|---|
| `项目说明.md` | 了解系统定位、核心模块、当前状态 | 项目级总览入口 |
| `产品愿景.md` | 判断功能是否符合长期方向 | 描述从内容中台到行业通用系统的长期目标 |
| `产品路线图.md` | 判断功能属于 V1 / V1.5 / V2 / V3 哪个阶段 | 包含当前权限治理、Workflow Engine、协作编辑等路线状态 |
| `功能优先级.md` | 判断任务优先级和是否应当前投入 | 用于避免过早泛化或无关扩张 |
| `中台系统验收清单.md` | 判断“完整新媒体中台系统”是否可被证据证明 | 按 P0 / P1 / P2 定义验收维度和证据来源 |

## 架构与数据

| 文档 | 适用场景 | 说明 |
|---|---|---|
| `系统架构.md` | 涉及前后端结构、实时通信、部署、Workflow Engine、协作层 | 架构主文档 |
| `ARCHITECTURE_CURRENT.md` | 长期架构升级前的现状核验 | 记录 2026-07-28 仓库审计事实、模块关系与分级问题 |
| `ARCHITECTURE_UPGRADE_PLAN.md` | Web + Android + iOS 长期演进 | 记录五阶段目标架构、兼容策略、风险与退出条件 |
| `PHASE2_TOPIC_DESIGN.md` | Topic 模块化实施前 | 记录接口、关系、权限、Repository、Service、Schema、v1 兼容、测试与回滚设计 |
| `API_CONTRACT.md` | 新增或改造 `/api/v1/*` 接口 | 统一版本、响应 envelope、错误码、HTTP 状态码、requestId 与 OpenAPI 规则 |
| `AUTH_CURRENT.md` | 涉及登录、JWT、用户状态、前端 token 或 Socket 认证 | 记录认证拆分前的数据流、存储方式、撤销能力与分级风险 |
| `PHASE2_AUTH_DESIGN.md` | Auth 模块化或 Refresh Token 实施前 | 记录 Auth Module、会话模型、轮换、Web/App、Socket、迁移、测试与回滚设计；C2.5 已完成 legacy Auth 接口模块化收口 |
| `PHASE2_AUTH_SESSION_DESIGN.md` | Phase 2-C3 Session / Refresh Token 实施前 | 记录会话主模型选型、Refresh Token 轮换与重放防护、Web/Mobile/Socket、v1 API、灰度、回滚和测试设计 |
| `PHASE2_AUTH_WEB_MIGRATION_DESIGN.md` | Phase 2-C3-5 Web 迁移实施前 | 记录 Web 现状、Cookie、CSRF、Pepper、灰度、登录事务、前端分阶段与 Socket/Yjs 交接方案 |
| `AUTH_SOCKET_MIGRATION_DESIGN.md` | Socket/Yjs Auth Bridge 迁移设计 | 记录 SocketAuthContext、Bridge 阶段、Token 生命周期、Room/Yjs 恢复和回滚边界 |
| `AUTH_SOCKET_TEST_PLAN.md` | Socket/Yjs 迁移测试 | 记录握手、重连、Room 恢复、Yjs 最终一致、多标签和回滚测试 |
| `releases/v2.13.18.md` | 查看 Phase 2-C3-7-C 发布边界 | 记录 Socket Coordinator、Yjs Recovery Bridge、默认关闭和未切换范围 |
| `releases/v2.13.4.md` | 查看 Phase 2-C3-2 发布边界 | 记录 Auth Session 数据库基础设施、兼容性和回滚边界 |
| `releases/v2.13.5.md` | 查看 Phase 2-C3-3 发布边界 | 记录 Session/Refresh Token 运行时内核、事务与未接入范围 |
| `releases/v2.13.6.md` | 查看 Phase 2-C3-4 发布边界 | 记录 Auth v1 实验接口、Feature Flag、契约、安全和兼容范围 |
| `releases/v2.13.7.md` | 查看 Phase 2-C3-5-B 发布边界 | 记录 Web Auth Runtime、api-client、Cookie/CSRF、灰度基础及未切换范围 |
| `releases/v2.13.8.md` | 查看 Phase 2-C3-5-C 发布边界 | 记录 Web Cookie/CSRF HTTP 契约、登录事务、测试与兼容范围 |
| `releases/v2.13.9.md` | 查看 Phase 2-C3-5-D 发布边界 | 记录 Web Auth 暗启浏览器验证、并发刷新、失败与未切换范围 |
| `releases/v2.13.10.md` | 查看 Phase 2-C3-5-E1 发布边界 | 记录 Auth 灰度模型、迁移指标、结构化事件与配置回滚方案 |
| `AUTH_ROLLOUT_RUNBOOK.md` | Auth 灰度上线、观察、停止与回滚 | 记录上线前检查、灰度步骤、指标、停止条件和责任人清单 |
| `releases/v2.13.11.md` | 查看 Phase 2-C3-5-E2 发布边界 | 记录 Auth 灰度运行诊断、时间指标、审计、阈值和只读管理页 |
| `releases/v2.13.12.md` | 查看 Phase 2-C3-5-E3 发布边界 | 记录生产 Auth v1-web 独立批准、allowlist 准入与回滚边界 |
| `releases/v2.13.13.md` | 查看 Phase 2-C3-6-A 发布边界 | 记录统一 Auth Event、指标唯一口径、Exporter 抽象与诊断窗口 |
| `AUTH_ALERT_RULES.md` | 配置 Auth 生产监控告警 | 记录 Refresh 失败率、Token reuse、CSRF、Expired 告警口径与处置 |
| `releases/v2.13.14.md` | 查看 Phase 2-C3-6-B 发布边界 | 记录 Prometheus/OTel Exporter、Registry、诊断与告警基础 |
| `AUTH_METRICS_MULTI_INSTANCE.md` | 部署多实例 Auth 指标 | 记录 instance 标签、Counter/Histogram 聚合和 active_sessions 限制 |
| `AUTH_LOGIN_MIGRATION_PLAN.md` | 评审正式 Login 准入 | 记录 legacy/v1 目标、迁移阶段、灰度、回滚、Socket 影响与风险 |
| `releases/v2.13.15.md` | 查看 Phase 2-C3-6-C 发布边界 | 记录内网 metrics endpoint、Collector 验证、多实例与 Login 设计 |
| `SOCKET_AUTH_CURRENT.md` | 审计 Socket/Yjs 认证现状 | 记录 handshake、Token、middleware、Room/Yjs 恢复与 legacy 依赖 |
| `AUTH_SOCKET_MIGRATION_DESIGN.md` | 实施 Socket Auth Bridge 前 | 记录 Context、v1 handshake、Refresh 重连、Yjs 恢复、兼容与回滚 |
| `AUTH_SOCKET_TEST_PLAN.md` | 规划 Socket/Yjs Auth 验证 | 覆盖到期、断网、Room/Yjs 恢复、多标签和回滚 |
| `releases/v2.13.16.md` | 查看 Phase 2-C3-7-A 发布边界 | 记录 Socket/Yjs Auth Bridge 设计范围与实施门禁 |
| `releases/v2.13.17.md` | 查看 Phase 2-C3-7-B 发布边界 | 记录 Socket Auth Bridge 模块、Feature Flag、身份绑定与测试 |
| `AUTH_PRODUCTION_GRAY_REPORT.md` | 查看生产 Auth 灰度执行证据 | 记录专用测试账号、真实浏览器结果、指标、异常与最终决策 |
| `CHANGELOG.md` | 查看标准版本技术更新 | 按版本记录新增、优化、修复、技术升级、数据库变化与测试情况 |
| `SYSTEM_UPDATE.md` | 面向非技术人员查看升级影响 | 使用业务语言说明当前版本、升级内容、使用和数据影响 |
| `UPGRADE_PROGRESS.md` | 查看长期升级阶段状态 | 记录已完成内容、文件、数据库变化、测试、风险与下一步 |
| `../deploy/UBUNTU_ECS_DEPLOY.md` | 涉及阿里云 ECS 正式环境、上线更新、回滚、备份 | 云端生产运维文档 |
| `数据库设计.md` | 涉及数据库表、字段、初始化脚本、运行时迁移 | 必须结合 `api/database/db.ts` 校验 |
| `数据库语义冻结层.md` | 涉及数据库事实来源、不可随意改动的数据语义 | 用于避免把观测表、业务事实表、兼容字段混用 |
| `语义统一规范.md` | 涉及内容字段、Workflow 事实来源、协作状态语义 | 当前内容字段和协作语义的冻结入口 |

## 开发与重构规范

| 文档 | 适用场景 | 说明 |
|---|---|---|
| `Codex开发规范.md` | 所有 Codex 任务 | 规定 Docs-first / Docs-last、风险控制和验证要求 |
| `重构规划.md` | 涉及结构治理、模块拆分、历史兼容 | 判断是否属于当前治理阶段 |
| `组件规范.md` | 涉及通用组件、页面状态、编辑器入口、Workflow UI | 组件使用和抽象边界 |
| `UI设计规范.md` | 涉及页面布局、交互、视觉统一 | UI 改动前必须优先查看 |

## 权限治理

| 文档 | 适用场景 | 说明 |
|---|---|---|
| `权限治理现状盘点.md` | 涉及角色、权限点、接口守卫、数据归属 | 当前权限真实状态入口 |
| `权限矩阵.md` | 判断 admin / director / editor / member 能做什么 | 权限点与角色矩阵 |
| `前后端权限对齐表.md` | 涉及页面、按钮、后端接口权限对齐 | 判断前端入口和后端守卫是否一致 |

当前权限治理状态：

1. 敏感只读治理 API 已完成后端权限收口。
2. 选题、创作、资源库、排期日历已冻结为数据归属优先模型。
3. 评论创建已接入 `workflow:comment`。
4. 灵感删除已统一为 owner-or-permission。
5. 选题创建已接入 `topic:create`。
6. 选题状态变更仍保留 `canAccessTopic + 状态机`，后续如拆分关键状态流转再评估 `topic:status`。

## 编辑器与协作

| 文档 | 适用场景 | 说明 |
|---|---|---|
| `编辑器运行时规则.md` | 涉及 ContentEditor、底层 Tiptap Editor、编辑器引用边界 | 编辑器运行时主入口 |
| `编辑器写入一致性规范.md` | 涉及 Yjs / database / snapshot 写入路径 | 防止多写路径和状态漂移 |
| `语义统一规范.md` | 涉及 `production.content`、`shooting.script_content`、`collaborationKey` | 内容字段和协作状态语义 |

编辑器相关任务必须优先确认：

1. 是否影响 `ContentEditor` 主入口。
2. 是否影响 `production.content` 或 `shooting.script_content`。
3. 是否影响 Yjs runtime、snapshot、writeConsistency。
4. 是否需要同步编辑器运行时或写入一致性文档。

## Workflow Engine

| 文档 | 适用场景 | 说明 |
|---|---|---|
| `系统架构.md` | Workflow Engine 当前运行状态、架构关系 | 当前为 `v2_strict` 强约束状态 |
| `数据库设计.md` | `workflow_shadow_logs`、`approval_records` 等数据语义 | 区分观测事实和业务事实 |
| `数据库语义冻结层.md` | Workflow 相关表不可混用的语义边界 | 防止把 shadow logs 当作审批事实 |
| `Codex开发规范.md` | Workflow 修改前确认事项 | 修改 policy、decision、strict gate 前必须确认影响面 |

Workflow 相关任务必须确认：

1. 是否改变状态转换规则。
2. 是否改变审批 API 阻断条件。
3. 是否改变 WorkflowDesigner 保存策略。
4. 是否改变 `topic.status` 或审批记录语义。
5. 是否需要更新 Workflow Engine 文档口径。

## 常见任务阅读路径

### 权限任务

1. `Codex开发规范.md`
2. `权限治理现状盘点.md`
3. `权限矩阵.md`
4. `前后端权限对齐表.md`
5. 相关路由、页面、权限 Hook

### 数据库任务

1. `Codex开发规范.md`
2. `数据库设计.md`
3. `数据库语义冻结层.md`
4. `语义统一规范.md`
5. `api/database/db.ts`

### 编辑器任务

1. `Codex开发规范.md`
2. `编辑器运行时规则.md`
3. `编辑器写入一致性规范.md`
4. `语义统一规范.md`
5. `ContentEditor`、协作层、业务详情页

### UI / 页面任务

1. `Codex开发规范.md`
2. `UI设计规范.md`
3. `组件规范.md`
4. `重构规划.md`
5. `中台系统验收清单.md`
6. 目标页面和复用组件

### Workflow 任务

1. `Codex开发规范.md`
2. `系统架构.md`
3. `数据库设计.md`
4. `数据库语义冻结层.md`
5. `shared/workflow/*`、`workflow-templates`、`WorkflowDesigner`

### 认证任务

1. `Codex开发规范.md`
2. `ARCHITECTURE_CURRENT.md`
3. `AUTH_CURRENT.md`
4. `PHASE2_AUTH_DESIGN.md`
5. `API_CONTRACT.md`
6. `api/routes/auth.ts`、`api/middleware/auth.ts`、`api/utils/jwt.ts`、Socket 与前端认证代码

## 维护规则

1. 新增重要文档时，应同步更新本文档。
2. 删除、合并或重命名文档时，应同步更新本文档。
3. 如果某个任务发现文档与代码不一致，应优先修正文档口径或在任务总结中明确说明。
4. 规划内容必须标注为规划，不能写成已实现。
5. 当前状态必须来自真实代码、真实数据库脚本或已经验证的运行行为。
6. 判断阶段性能力是否完成时，应优先参考 `中台系统验收清单.md` 的证据要求。
