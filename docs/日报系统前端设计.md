# 日报系统前端设计

## 页面结构

- `/daily-report` 使用三个 Tab：我的日报、团队日报、日报归档。
- 普通用户显示“我的日报”和“日报归档”。
- 管理员、编导或具备团队/审核权限的用户显示“团队日报”。

## 组件结构

- `DailyReportPage.tsx`：页面状态、接口编排、Tab 切换。
- `DailyReportComposer.tsx`：个人日报填写、保存草稿、提交。
- `DailyReportAutoSummaryPanel.tsx`：自动草稿建议展示和追加。
- `DailyReportTeamBoard.tsx`：团队日报列表与审核入口。
- `DailyReportArchiveList.tsx`：个人和管理视角归档查询。
- `DailyReportReviewDialog.tsx`：通过/退回审核弹窗。
- `DailyReportStatusPill.tsx`：状态和风险标签。

## 第 11 轮验收状态

- `npm run check` 通过。
- `npm run build` 通过。
- 日报真实 token API smoke 通过。
- 登录态 E2E smoke 通过。
- 登录后 `/daily-report` 可打开。
- 未完成浏览器手工填写六段、保存、提交、审核深度流程。

