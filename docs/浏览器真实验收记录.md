# 浏览器真实验收记录

## 第 11 轮真实 E2E 记录

- 日期：2026-07-03。
- 前端地址：`http://localhost:5174`。
- 后端地址：`http://localhost:3001/api`。
- Playwright Chromium：已安装到用户本机缓存目录，不在仓库内。
- 登录态 E2E：通过。
- 登录后 `/daily-report`：可打开。
- 登录后 `/retrospectives`：可打开。
- 暗色模式 smoke：通过。
- API health：smoke 后为 `ok`。
- 安全说明：未记录账号、密码或 token 明文。

## 已完成

- 登录页可打开。
- 受保护路由门槛可用。
- 登录态路由 smoke 可用。
- 日报 API smoke 真实 token 链路通过。
- 复盘 API smoke 真实 token 链路通过。

## 未完成

- 日报浏览器手工填写六段、保存、提交、审核深度流程。
- 复盘浏览器手工创建、快照、行动项、导出、发布、归档深度流程。
- 编辑器主题登录后截图验收。
- 布局滚动登录后截图验收。
- 移动端导航抽屉实测。

说明：E2E smoke 通过不等于所有手工深度流程已全量验收。

## 第 12 轮中文化专项记录

- 目标：检查 `/daily-report`、`/retrospectives`、`/retrospectives/:id`、导出 Markdown、HTML 打印页、暗色模式和移动端是否仍有用户可见英文、技术解释文案或 raw enum。
- 已在代码层移除复盘中心页和复盘列表中的 `analytics` 技术解释文案。
- 已将日报详情、自动草稿、复盘指标快照、日报风险联动、导出内容和消息提醒中的主要英文/枚举展示改为中文。
- `npm run test:e2e` 登录态分支通过，确认登录后 `/daily-report` 和 `/retrospectives` 可打开，暗色模式 smoke 通过。
- Markdown 导出 API 返回 200，内容为中文复盘信息，未发现 `Smoke retrospective`、`analytics` 或 raw enum。
- HTML 打印页导出 API 返回 200，内容为中文复盘信息，未发现 `Smoke retrospective`、`analytics` 或 raw enum。
- 最终 Playwright 深度补测已覆盖 `/daily-report`、`/retrospectives`、`/retrospectives/:id`。
- 复盘详情页 Markdown 导出按钮和 HTML 打印页入口可点击。
- 暗色模式已覆盖 `/daily-report`、`/retrospectives`、`/retrospectives/:id`。
- 移动端窄屏已覆盖 `/daily-report`、`/retrospectives`、`/retrospectives/:id`。
- 本次深度补测未发现应用级 console error。
- 安全说明：未在文档中记录账号、密码或 token 明文。
