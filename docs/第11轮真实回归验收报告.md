# 第 11 轮真实回归验收报告

## 基本信息

- 日期：2026-07-03。
- 目标：真实测试账号登录态补测、日报/复盘 smoke 全链路、浏览器 E2E、提交前最终确认。
- 本轮未新增业务功能，未修改数据库 schema，未提交 Git，未 push，未部署生产。
- 本报告不记录真实账号、密码或 token 明文。

## 登录与 Token

- 是否尝试登录：是。
- 登录接口：`POST /api/auth/login`。
- 是否登录成功：是。
- 登录角色：admin。
- 是否需要强制改密：否。
- 是否获取 `TOKEN`：是。
- 是否获取 `ADMIN_TOKEN`：是。
- 是否记录账号密码明文：否。
- 是否记录 token 明文：否。

## 日报 Smoke

- 是否执行真实 `TOKEN` 链路：是。
- 是否执行 `ADMIN_TOKEN` 链路：是。
- 结果：通过。
- 覆盖接口：我的日报查询、非法日期校验、空日报提交校验、保存草稿、版本冲突校验、提交日报、归档查询、归档范围校验、团队日报查询、审核通过。
- 失败接口：无。
- 未覆盖：浏览器手工填写六段、保存、提交、审核的深度 UI 流程。

## 复盘 Smoke

- 是否执行真实 `TOKEN` 链路：是。
- 是否执行 `ADMIN_TOKEN` 链路：是。
- 结果：通过。
- 覆盖接口：模板列表、复盘列表、非法创建校验、创建复盘、详情查询、版本冲突校验、更新复盘、生成快照、创建行动项、更新行动项、发布复盘、归档复盘、归档后只读校验。
- 失败接口：无。
- 未覆盖：浏览器手工创建、快照、行动项、导出、发布、归档的深度 UI 流程。

## E2E

- Playwright Chromium：已安装到用户本机缓存目录，不在仓库内。
- 是否执行 `npm run test:e2e`：是。
- 登录态分支：通过。
- 登录后 `/daily-report`：可打开。
- 登录后 `/retrospectives`：可打开。
- 暗色模式 smoke：通过。
- API health：smoke 后为 `ok`。

## 本轮小范围修复

- `api/services/retrospectives.ts`：复盘发布提醒查询从不存在的 `users.status` 改为当前 schema 存在的 `enabled = 1`，避免发布后通知逻辑导致 API 崩溃。
- `scripts/smoke-daily-reports.mjs`：日报 smoke 使用唯一未来日期，避免重复运行命中已提交日报导致 409。

## 补丁后命令结果

- `git status --short`：存在多轮未提交改动，未发现 `.env`、`data/xmt.db`、`dist`、Playwright 浏览器二进制进入待提交列表。
- `git diff --stat`：tracked diff 为 12 个文件，465 insertions / 227 deletions；不含 untracked 文件。
- `npm run check`：通过。
- `npm run build`：通过，仅 Vite chunk size warning。
- `npm run lint`：失败，仍为既有 238 个问题。
- 专项 eslint：通过。
- 敏感信息扫描：未发现真实 token、真实密码或私钥写入仓库文件。

## Git 提交建议

- 可以进入 Git commit 准备阶段。
- 建议拆分 6 个 commit：
  1. `fix: improve editor theme and layout scrolling`
  2. `feat: add daily report backend and frontend`
  3. `feat: add retrospective backend and frontend`
  4. `feat: add retrospective action workflow and exports`
  5. `test: add smoke tests and ci baseline`
  6. `docs: add rollout and validation documents`
- 不建议本轮直接 push，除非用户明确确认分支、提交拆分和 PR 目标。

## 生产部署建议

- 不建议现在直接部署生产。
- 可以进入部署准备，但生产前必须确认：
  - 完成或明确接受手工深度浏览器验收不足风险。
  - 生产 SQLite 已备份。
  - 当前 commit 已记录。
  - PM2 状态已记录。
  - 回滚方案已确认。
  - 用户明确授权部署。

## 未完成事项

- 全仓 lint 债务。
- 手工深度浏览器验收。
- 复盘模板管理页。
- 逾期行动项定时提醒。
- 订阅偏好。
- 外部平台真实数据。

