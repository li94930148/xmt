# 第 9 轮真实回归验收报告

## 基本信息

- 日期：2026-07-02。
- 范围：真实登录态回归测试、浏览器 E2E 验收、CI 基线、上线前质量整理。
- 禁止项遵守：未部署生产，未提交 Git，未写入账号、密码或 token，未改数据库结构，未接入外部平台数据。

## 命令结果

- `git status --short`：存在第 7、8、9 轮未提交变更。
- `npm run check`：通过。
- `npm run build`：通过，仅有 Vite chunk size 提示。
- `npm run lint`：失败，仍为既有 238 个问题。
- `npm run test:daily-reports`：无 `TOKEN` 友好提示分支通过。
- `npm run test:retrospectives`：无 `TOKEN` 友好提示分支通过。
- `npm run test:e2e`：Playwright 浏览器二进制未安装，脚本友好跳过并提示安装 Chromium 后可启用。

## Smoke 结果

- 日报 smoke：未获得真实 token，未执行完整 API 写入链路。
- 复盘 smoke：未获得真实 token，未执行完整 API 写入链路。
- 说明：没有伪造测试结果，没有记录 token 明文。

## 浏览器验收结果

- 当前无可安全复用的登录态。
- 未声明 `/daily-report`、`/retrospectives` 登录后主流程已通过。
- 已新增轻量 E2E 脚本，支持有账号时执行登录后路由冒烟；缺少账号时验证登录页与受保护路由门槛。
- 应用内浏览器已验证 `/login` 可打开，页面标题为“新媒体协作管理系统”，页面非空，无应用级 console error。
- 应用内浏览器已验证 `/daily-report`、`/retrospectives` 在无登录态下会回到 `/login`，说明受保护路由门槛生效。

## CI 基线

- 已新增 `.github/workflows/ci.yml`。
- CI 执行 `npm ci`、`npm run check`、`npm run build`。
- 暂不把全仓 `npm run lint` 纳入阻塞项。
- 暂不把需要 token 的 smoke 或 E2E 放入 CI。

## 第 10 轮补充

- 再次确认当前环境没有 `TOKEN`、`ADMIN_TOKEN`、`E2E_USERNAME`、`E2E_PASSWORD`。
- 日报和复盘 smoke 仍只覆盖无 token 友好提示分支。
- E2E 仍因缺少 Playwright Chromium 二进制友好跳过。
- 真实登录后全链路仍待提供本地测试账号或临时 token 后补测。

## 未通过 / 未验收项

- 真实 token 日报 smoke。
- 真实 token 复盘 smoke。
- 浏览器登录后日报主流程。
- 浏览器登录后复盘主流程。
- 编辑器主题截图验收。
- 布局滚动截图验收。
- 移动端导航实机或模拟器验收。

## 下一步建议

- 提供本地临时测试账号或浏览器登录态后，补跑 smoke 和 E2E。
- 逐步清理 lint 债务，并新增专项 lint 脚本。
- 准备隔离测试数据库，再把 E2E 扩展到日报提交、复盘创建、导出等写入链路。

