import { X } from 'lucide-react';
import type { DailyReport } from '../../api/dailyReports';

export default function DailyReportDetailDrawer({ report, onClose }: { report: DailyReport | null; onClose: () => void }) {
  if (!report) return null;
  return <div className="fixed inset-0 z-50"><button type="button" aria-label="关闭日报详情" className="absolute inset-0 bg-black/45" onClick={onClose} /><aside className="absolute right-0 top-0 flex h-full w-full max-w-2xl flex-col border-l border-studio-border-soft bg-studio-surface text-studio-text-primary shadow-2xl"><div className="flex items-start justify-between border-b border-studio-border-soft px-5 py-4"><div><p className="text-sm text-studio-text-muted">日报内容</p><h2 className="mt-1 text-xl font-semibold">{report.userName || report.username || `成员 ${report.userId}`}</h2><p className="mt-2 text-sm text-studio-text-muted">{report.reportDate}</p></div><button type="button" onClick={onClose} className="rounded-button border border-studio-border-soft p-2 text-studio-text-muted" aria-label="关闭"><X className="h-4 w-4" /></button></div><div className="min-h-0 flex-1 overflow-y-auto space-y-4 px-5 py-5">{report.items.map((item) => <section key={item.sectionKey} className="rounded-card border border-studio-border-soft bg-white/[0.035] p-4"><h3 className="text-sm font-semibold">{item.title || item.sectionKey}</h3><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-studio-text-secondary">{item.contentMd || '未填写内容'}</p></section>)}</div></aside></div>;
}
